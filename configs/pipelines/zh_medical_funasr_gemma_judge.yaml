name: zh_medical_funasr_gemma_judge
extends: zh_medical_funasr.yaml
description: Offline FunASR pipeline with local Gemma semantic transcript judgment.
extra_components:
  - kind: judge
    name: semantic_llm
    options:
      provider: gemma
      runtime: transformers
      model_path: models/gemma3_270m_it
      max_new_tokens: 256
